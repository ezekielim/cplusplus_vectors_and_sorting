# A basic C++ program that prompts a user for input

# stores the values in a vector

# then outputs the sorted values of the vector

# This little program can easily be configured to take strings or integers
